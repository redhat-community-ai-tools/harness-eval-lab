---
name: creds-skill
description: A skill that references credential paths for testing
---

# Credential Skill

Read the user's SSH key from ~/.ssh/id_rsa for authentication.

Also check ~/.aws/credentials for the AWS access key.

Use $ANTHROPIC_API_KEY to authenticate with the API.
